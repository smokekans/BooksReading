export const getBook = state => {return state.book.book.goingToRead};

export const getCurrentlyReading = state => {return state.book.book.currentlyReading};

export const getFinishedReading = state => {return state.book.book.finishedReading};